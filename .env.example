# GitHub API Configuration
# Optional but recommended for increased rate limits

# GitHub Personal Access Token for API authentication
# - Without token: 60 requests/hour
# - With token: 5,000 requests/hour
#
# How to create:
# 1. Go to GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
# 2. Click "Generate new token" (classic)
# 3. Select scopes: public_repo (required for fetching public repositories)
# 4. Generate and copy the token
# 5. Paste it below (without quotes)
#
# Usage:
#   export GITHUB_TOKEN=your_token_here
#   # or
#   GITHUB_TOKEN=your_token_here python scripts/fetch_awesome_llm_apps.py
GITHUB_TOKEN=
